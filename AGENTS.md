# Workspace rules

- 保持用户在 `3002` 端口运行的本地开发服务；除非用户明确要求，不得启动、停止、重启、替换该进程或修改端口。
- 除非用户明确要求，不得执行 `git add`、提交、推送、创建 PR 或任何部署操作。
- 不得重新添加 `.openai/hosting.json`、ChatGPT Sites 打包插件或其他自动部署入口，除非用户明确要求恢复。
- 开发服务运行期间优先使用不会中断热更新的检查方式；不得为了验证而接管本地端口。
